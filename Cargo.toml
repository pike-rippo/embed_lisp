[package]
name = "embed_lisp"
version = "0.1.0"
edition = "2024"

[dependencies]
colorize = { path = "C:/Users/rippo/pro/rs/colorize" }
thiserror = "2.0.17"
tokio = { version = "1.48.0", features = ["full"] }

[features]
# default = []
default = ["async"]
async = []
